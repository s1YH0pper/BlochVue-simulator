<!--
  Changelog format follows "Keep a Changelog" and Semantic Versioning.
  See: https://keepachangelog.com/ and https://semver.org/
-->

# 更新日志

本项目的所有显著变更将记录在此文件中。

## [Unreleased]

### Added

### Changed

### Fixed

---

## [0.1.3] - 2026-05-30

### Added

- 新增统一的 `DialogManager`，集中管理帮助、性能监控和设置入口。
- 新增性能监控面板，支持显示场景 FPS、渲染帧时、内存占用，以及平均/最小/最大 FPS 统计。
- 新增 Three.js 渲染统计工具，用于按 `WebGLRenderer.render()` 调用次数计算真实场景渲染 FPS。
- 新增设置对话框，支持启动时显示帮助、启动时显示性能监控、性能模式选择、目标帧率设置、恢复默认设置和清除缓存。
- 新增用户设置持久化能力，通过 `localStorage` 保存显示和性能相关配置。

### Changed

- 将原有帮助弹窗入口整合进 `DialogManager`，为后续弹窗和工具入口提供统一管理方式。
- `AnimationManager` 支持动态目标帧率，不再依赖固定配置中的帧间隔。
- 设置中的目标 FPS 会转换为动画帧间隔，用于控制渲染更新节奏。
- 性能监控开关与设置状态联动，用户偏好可在下次启动时恢复。
- 优化设置弹窗在桌面、平板和移动端下的响应式宽度与表单布局。

---

## [0.1.2] - 2026-4-9

### Added

- **保存按钮状态联动**：新增对 appState.savedFlag 的监听，用于动态更新保存按钮标签。
- **快捷键说明增强**：在帮助对话框中新增保存、恢复与场景切换相关的快捷键说明。
- **UIEvents 场景管理功能增强**：为 UIEvents 组件新增场景保存与恢复功能，并同步更新快捷键逻辑。
- **地板形态切换功能**：新增地板形态切换功能，并更新对应材质颜色。

### Changed

- **键盘事件处理优化**：重构键盘事件处理逻辑，统一大小写判断规则，提升一致性与可维护性。

---

## [0.1.1] - 2025-12-13

### Added

- **新增切换帮助对话框的快捷键**：为 HelpDialog 增加快捷键开关，提高操作效率。
- **场景切换的快捷键增强**：扩展快捷键系统，使其支持不同场景间的快速切换操作。
- **配置支持 Axis Helper**：支持在配置中启用坐标轴辅助工具，增强可视化调试能力。
- **SceneManager 中新增 Axis Helper 初始化**：在场景初始化生命周期中添加 Axis Helper 的自动创建逻辑。
- **OrbitControlDrawer 组件**：新增用于摄像机控制与 Axis Helper 显示切换的侧边抽屉组件。

---

## [0.1.0] - 2025-11-10

### Added

- **帮助系统**：交互式帮助对话框，包含快捷键说明、操作指南、场景说明和项目信息
- **键盘快捷键系统**：简单的键盘快捷键支持
- **全局错误与性能检测**：新增统一的错误捕获与性能检测工具，便于调试与优化

### Changed

- **工具函数迁移**：将通用函数迁移至 threeTools.js，并更新相关导入路径

---

## [0.1.0-beta.2] - 2025-10-17

### Added

- 场景切换过程中暂停场景的更新
- 新增版本变更日志文件，用于记录后续版本历史

### Changed

- 重构视图菜单的布局结构
- 在 README 中补充安装步骤、使用方法与组件结构说明

### Fixed

- 修复切换场景时 FID 信号未正确清空的问题
- 修复恢复场景后 FID 渲染状态错误的问题

---

## [0.1.0-beta.1] - 2025-10-13

### Added

- 场景保存与恢复功能（切换后可一键恢复）
- 切换场景确认对话框，避免误操作丢失状态

### Changed

- 代码结构精简：移除未使用的窗口尺寸监听逻辑
- 替换直接 DOM 操作为更规范的 Vue 方式

### Performance

- 使用 `toRaw` 优化 `IsocArr` 响应式性能
- 优化 `saveState`：使用展开运算符进行高效状态克隆

### Known Issues

- FID 组件：使用恢复场景功能时曲线渲染可能异常
- 移动端适配尚未完成

---

## [0.1.0-alpha.2] - 2025-10-10

### Changed

- 多个性能关键模块优化，提升大规模场景渲染与物理计算效率
- 本地化与交互细节优化

### Performance

- 减少 `updateB1AndIsochromats` 冗余计算
- 提升 `PhysicalParam` 参数更新效率
- 降低 `BlochStep` 计算开销
- 加速 `relaxThermal` 热弛豫计算
- 提升 `updateFid` 性能（减少数组移位）
- 优化场景对象移除逻辑以提升切换响应速度

### Fixed

- 改善 `appState` 状态保存/恢复稳定性
- 改善 `appState` 定时器管理以避免重复触发与资源冲突

### Other

- 重新设计应用图标

---

## 参考

- 格式参考：Keep a Changelog
- 版本规范：Semantic Versioning
